This repository is being used to dogfood `sao` against a real GitHub issue.

The goal is simple: let `sao` discover an issue in this repo, execute the work locally, and validate the end-to-end loop on a small, real task.
